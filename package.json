{
  "name": "pq-reps",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.server.json && cp -R src/ui dist/ui",
    "start": "node --enable-source-maps dist/server.js",
    "test": "node --test --import tsx tests/generate-api.test.ts",
    "logs": "./scripts/logs.sh",
    "logs:6h": "./scripts/logs.sh 6h"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "tsx": "^4.7.2",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "dotenv": "^17.2.3"
  }
}
